<Window x:Class="GenshinPiano.App.Dialogs.RenameScoreDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Dialog_RenameScoreTitle}"
        Width="390" Height="190"
        WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Padding="14,0,6,0"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
                <DockPanel>
                    <Button DockPanel.Dock="Right" Width="32" Height="28" Padding="0"
                            Content="✕" Background="Transparent" BorderThickness="0"
                            Click="CancelButton_OnClick" />
                    <TextBlock VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"
                               Text="{DynamicResource Dialog_RenameScoreTitle}" />
                </DockPanel>
            </Border>

            <Grid Grid.Row="1" Margin="20,16,20,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="{DynamicResource Dialog_RenameScoreLabel}"
                           Foreground="{DynamicResource SecondaryTextBrush}" />
                <TextBox x:Name="NameTextBox" Grid.Row="1" Height="34" Margin="0,6,0,0"
                         Padding="9,5" VerticalContentAlignment="Center"
                         Background="{DynamicResource SurfaceBrush}"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         TextChanged="NameTextBox_OnTextChanged" />
                <TextBlock x:Name="ErrorText" Grid.Row="2" Margin="0,5,0,0"
                           FontSize="11" Foreground="#E57373" />
                <StackPanel Grid.Row="3" HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Content="{DynamicResource Common_Cancel}" Style="{StaticResource AppButtonStyle}"
                            IsCancel="True" Click="CancelButton_OnClick" />
                    <Button x:Name="RenameButton" Content="{DynamicResource Action_Rename}"
                            Style="{StaticResource PrimaryButtonStyle}" IsDefault="True"
                            Click="RenameButton_OnClick" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
