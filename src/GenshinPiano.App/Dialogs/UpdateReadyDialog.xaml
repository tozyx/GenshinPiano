<Window x:Class="GenshinPiano.App.Dialogs.UpdateReadyDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Width="430" SizeToContent="Height" WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="{DynamicResource AppFontFamily}" ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
  <Border Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="24">
    <StackPanel>
      <TextBlock Text="{DynamicResource Update_ReadyTitle}" FontSize="18" FontWeight="SemiBold"/>
      <TextBlock x:Name="MessageText" Margin="0,12,0,22" TextWrapping="Wrap" Foreground="{DynamicResource SecondaryTextBrush}"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Width="100" Height="36" Content="{DynamicResource Update_Later}" Style="{StaticResource AppButtonStyle}" Click="Later_OnClick"/>
        <Button Width="130" Height="36" Margin="8,0,0,0" Content="{DynamicResource Update_Restart}" Style="{StaticResource PrimaryButtonStyle}" Click="Restart_OnClick"/>
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
