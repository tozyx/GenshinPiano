<Window x:Class="GenshinPiano.App.Dialogs.ReleaseNotesDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Width="520" Height="380" WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="{DynamicResource AppFontFamily}" ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
  <Border Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="24">
    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <TextBlock x:Name="TitleText" FontSize="19" FontWeight="SemiBold"/>
      <Border Grid.Row="1" Margin="0,16" Padding="14" Background="{DynamicResource SidebarBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
        <FlowDocumentScrollViewer x:Name="NotesViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  IsToolBarVisible="False"
                                  IsSelectionEnabled="False"
                                  Background="Transparent"
                                  BorderThickness="0" />
      </Border>
      <Button Grid.Row="2" Width="100" Height="36" HorizontalAlignment="Right" Content="{DynamicResource Update_Close}" Style="{StaticResource PrimaryButtonStyle}" Click="Close_OnClick"/>
    </Grid>
  </Border>
</Window>
